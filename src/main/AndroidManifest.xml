<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ust.leadsv2.androidlogin">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_leads_launcher"
        android:label="LEAdS"
        android:roundIcon="@mipmap/ic_leads_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SigninActivity" />
        <activity android:name=".ForgotPassword" />
        <activity android:name=".SignupActivity" />
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main" />
        <activity
            android:name=".ViewLogs"
            android:label="@string/title_activity_view_logs"
            android:theme="@style/AppTheme" />
        <activity android:name=".Notifications" />
        <activity android:name=".CurrentReadings" />
        <activity android:name=".Notif"></activity>
    </application>

</manifest>